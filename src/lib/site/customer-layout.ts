/** Offset for fixed customer header — keep in sync with CustomerHeader layout. */
export const customerMainOffsetClass =
  "pt-16 sm:pt-[5rem] md:pt-[7.25rem] lg:pt-[7.5rem]";

/** Standard inner page padding below the header offset. */
export const customerPageShellClass =
  "mx-auto max-w-7xl px-4 py-8 sm:px-6 sm:py-12 lg:py-14";
